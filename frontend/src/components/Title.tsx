const Title = ({ title }: { title: string }) => {
  return <h1 className="pb-4 border-b border-gray-400 text-2xl leading-normal text-black font-medium capitalize">{title}</h1>;
};

export default Title;
